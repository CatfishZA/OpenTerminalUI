"""Canonical simulation package introduced by Phase 1A.

The package root intentionally has no infrastructure imports so domain users do
not acquire FastAPI or SQLAlchemy dependencies as a side effect.
"""
